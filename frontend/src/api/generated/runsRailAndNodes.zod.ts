/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Atelier 2 durable workflow API
 * OpenAPI spec version: 1
 */
import * as zod from 'zod';

export const agentBindingResourceV2AgentConfigurationRevisionHashRegExp = new RegExp('^[0-9a-f]{64}$');
export const agentBindingResourceV2AuthProfileRevisionHashRegExp = new RegExp('^[0-9a-f]{64}$');
export const agentBindingResourceV2ExecutorRevisionMax = 1024;

export const agentBindingResourceV2ModelMax = 1024;

export const agentBindingResourceV2ProfileIdMax = 1024;

export const agentBindingResourceV2ProviderIdMax = 64;

export const agentBindingResourceV2RevisionNumberMax = 9223372036854776000;

export const agentBindingResourceV2RoleMax = 1024;



export const AgentBindingResourceV2 = zod.strictObject({
  "agent_configuration_revision_hash": zod.string().regex(agentBindingResourceV2AgentConfigurationRevisionHashRegExp),
  "auth_mode": zod.enum(['subscription', 'api_key']),
  "auth_profile_revision_hash": zod.string().regex(agentBindingResourceV2AuthProfileRevisionHashRegExp),
  "executor_revision": zod.string().min(1).max(agentBindingResourceV2ExecutorRevisionMax),
  "model": zod.string().min(1).max(agentBindingResourceV2ModelMax),
  "profile_id": zod.string().min(1).max(agentBindingResourceV2ProfileIdMax),
  "provider_id": zod.string().min(1).max(agentBindingResourceV2ProviderIdMax),
  "revision_number": zod.int().min(1).max(agentBindingResourceV2RevisionNumberMax),
  "role": zod.string().min(1).max(agentBindingResourceV2RoleMax)
});

export type AgentBindingResourceV2 = zod.input<typeof AgentBindingResourceV2>;
export type AgentBindingResourceV2Output = zod.output<typeof AgentBindingResourceV2>;

export const transcriptRecordedMomentResourceRecordedAtRegExp = new RegExp('^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$');


export const TranscriptRecordedMomentResource = zod.strictObject({
  "origin": zod.literal("recorded").meta({ title: 'Origin' }),
  "recorded_at": zod.string().regex(transcriptRecordedMomentResourceRecordedAtRegExp)
}).describe('The instant an event entered the transcript and the source of that instant.');

export type TranscriptRecordedMomentResource = zod.input<typeof TranscriptRecordedMomentResource>;
export type TranscriptRecordedMomentResourceOutput = zod.output<typeof TranscriptRecordedMomentResource>;

export const TranscriptBeforeMomentsResource = zod.strictObject({
  "origin": zod.literal("v1-before-moments").meta({ title: 'Origin' })
}).describe('An event read from a v1 transcript, before event moments existed.');

export type TranscriptBeforeMomentsResource = zod.input<typeof TranscriptBeforeMomentsResource>;
export type TranscriptBeforeMomentsResourceOutput = zod.output<typeof TranscriptBeforeMomentsResource>;

export const assistantTurnEventResourceTextMax = 8192;



export const AssistantTurnEventResource = zod.strictObject({
  "event": zod.literal("assistant-turn").meta({ title: 'Event' }),
  "moment": zod.union([TranscriptRecordedMomentResource,TranscriptBeforeMomentsResource]),
  "redacted": zod.boolean(),
  "text": zod.string().max(assistantTurnEventResourceTextMax)
});

export type AssistantTurnEventResource = zod.input<typeof AssistantTurnEventResource>;
export type AssistantTurnEventResourceOutput = zod.output<typeof AssistantTurnEventResource>;

export const toolCalledEventResourceArgumentsMax = 8192;

export const toolCalledEventResourceNameMax = 8192;



export const ToolCalledEventResource = zod.strictObject({
  "arguments": zod.string().max(toolCalledEventResourceArgumentsMax),
  "event": zod.literal("tool-called").meta({ title: 'Event' }),
  "moment": zod.union([TranscriptRecordedMomentResource,TranscriptBeforeMomentsResource]),
  "name": zod.string().max(toolCalledEventResourceNameMax),
  "redacted": zod.boolean()
});

export type ToolCalledEventResource = zod.input<typeof ToolCalledEventResource>;
export type ToolCalledEventResourceOutput = zod.output<typeof ToolCalledEventResource>;

export const toolReturnedEventResourceNameMax = 8192;

export const toolReturnedEventResourceResultMax = 8192;



export const ToolReturnedEventResource = zod.strictObject({
  "event": zod.literal("tool-returned").meta({ title: 'Event' }),
  "moment": zod.union([TranscriptRecordedMomentResource,TranscriptBeforeMomentsResource]),
  "name": zod.string().max(toolReturnedEventResourceNameMax),
  "redacted": zod.boolean(),
  "result": zod.string().max(toolReturnedEventResourceResultMax)
});

export type ToolReturnedEventResource = zod.input<typeof ToolReturnedEventResource>;
export type ToolReturnedEventResourceOutput = zod.output<typeof ToolReturnedEventResource>;

export const usageEventResourceCacheCreationInputTokensMin = 0;

export const usageEventResourceCacheReadInputTokensMin = 0;

export const usageEventResourceInputTokensMin = 0;

export const usageEventResourceOutputTokensMin = 0;



export const UsageEventResource = zod.strictObject({
  "cache_creation_input_tokens": zod.int().min(usageEventResourceCacheCreationInputTokensMin),
  "cache_read_input_tokens": zod.int().min(usageEventResourceCacheReadInputTokensMin),
  "event": zod.literal("usage").meta({ title: 'Event' }),
  "input_tokens": zod.int().min(usageEventResourceInputTokensMin),
  "moment": zod.union([TranscriptRecordedMomentResource,TranscriptBeforeMomentsResource]),
  "output_tokens": zod.int().min(usageEventResourceOutputTokensMin)
});

export type UsageEventResource = zod.input<typeof UsageEventResource>;
export type UsageEventResourceOutput = zod.output<typeof UsageEventResource>;

export const providerTerminalRefusalEventResourceApiErrorStatusMax = 8192;

export const providerTerminalRefusalEventResourceTerminalReasonMax = 8192;

export const providerTerminalRefusalEventResourceTextMax = 8192;



export const ProviderTerminalRefusalEventResource = zod.strictObject({
  "api_error_status": zod.string().max(providerTerminalRefusalEventResourceApiErrorStatusMax),
  "event": zod.literal("provider-terminal-refusal").meta({ title: 'Event' }),
  "moment": zod.union([TranscriptRecordedMomentResource,TranscriptBeforeMomentsResource]),
  "redacted": zod.boolean(),
  "terminal_reason": zod.string().max(providerTerminalRefusalEventResourceTerminalReasonMax),
  "text": zod.string().max(providerTerminalRefusalEventResourceTextMax)
});

export type ProviderTerminalRefusalEventResource = zod.input<typeof ProviderTerminalRefusalEventResource>;
export type ProviderTerminalRefusalEventResourceOutput = zod.output<typeof ProviderTerminalRefusalEventResource>;

export const unrecognisedProviderOutputEventResourceTextMax = 8192;



export const UnrecognisedProviderOutputEventResource = zod.strictObject({
  "event": zod.literal("unrecognised-provider-output").meta({ title: 'Event' }),
  "moment": zod.union([TranscriptRecordedMomentResource,TranscriptBeforeMomentsResource]),
  "redacted": zod.boolean(),
  "text": zod.string().max(unrecognisedProviderOutputEventResourceTextMax)
});

export type UnrecognisedProviderOutputEventResource = zod.input<typeof UnrecognisedProviderOutputEventResource>;
export type UnrecognisedProviderOutputEventResourceOutput = zod.output<typeof UnrecognisedProviderOutputEventResource>;




export const TranscriptTruncatedEventResource = zod.strictObject({
  "dropped_events": zod.int().min(1),
  "event": zod.literal("transcript-truncated").meta({ title: 'Event' }),
  "moment": zod.union([TranscriptRecordedMomentResource,TranscriptBeforeMomentsResource])
});

export type TranscriptTruncatedEventResource = zod.input<typeof TranscriptTruncatedEventResource>;
export type TranscriptTruncatedEventResourceOutput = zod.output<typeof TranscriptTruncatedEventResource>;




export const AttemptTranscriptResource = zod.strictObject({
  "events": zod.array(zod.union([ToolCalledEventResource,ToolReturnedEventResource,AssistantTurnEventResource,UsageEventResource,ProviderTerminalRefusalEventResource,UnrecognisedProviderOutputEventResource,TranscriptTruncatedEventResource])).min(1)
}).describe('The decoded, already-redacted steps of one attempt, and nothing else.\n\nThe document kind and the stored bytes stay off the wire: a reader of this\nresource is looking at the events, not at how they were kept.');

export type AttemptTranscriptResource = zod.input<typeof AttemptTranscriptResource>;
export type AttemptTranscriptResourceOutput = zod.output<typeof AttemptTranscriptResource>;

export const defectiveRunRowResourceDetailMax = 512;

export const defectiveRunRowResourcePublicRunReferenceRegExp = new RegExp('^run1\\.[A-Za-z0-9_-]+$');


export const DefectiveRunRowResource = zod.strictObject({
  "detail": zod.string().min(1).max(defectiveRunRowResourceDetailMax),
  "kind": zod.literal("defective").meta({ title: 'Kind' }),
  "problem_code": zod.literal("durable-state-corrupt").meta({ title: 'Problem Code' }),
  "public_run_reference": zod.string().regex(defectiveRunRowResourcePublicRunReferenceRegExp)
}).describe('A listed row for a run whose own projection failed (#1042).\n\nThe other rows on the same page prove nothing about this one: a list\ndies for none of them just because this run\'s own projection could not\nbe told, so this is the row it becomes instead. `problem_code` is the\nclosed, typed reason; `detail` is `bounded_run_row_defect_detail`\'s\ncurated, bounded reason -- never the run\'s own durable bytes, and never\nthe store exception\'s own message, which carries no bound of its own. The\nexception\'s full text stays in the per-run process journal entry only.');

export type DefectiveRunRowResource = zod.input<typeof DefectiveRunRowResource>;
export type DefectiveRunRowResourceOutput = zod.output<typeof DefectiveRunRowResource>;

export const nodeAnswerResourceValueHashRegExp = new RegExp('^[0-9a-f]{64}$');


export const NodeAnswerResource = zod.strictObject({
  "value_base64": zod.string(),
  "value_hash": zod.string().regex(nodeAnswerResourceValueHashRegExp)
}).describe('What one node wrote, as the run kept it.');

export type NodeAnswerResource = zod.input<typeof NodeAnswerResource>;
export type NodeAnswerResourceOutput = zod.output<typeof NodeAnswerResource>;

export const nodeProvenanceResourceAgentConfigurationRevisionHashRegExp = new RegExp('^[0-9a-f]{64}$');
export const nodeProvenanceResourceAuthModeMax = 1024;

export const nodeProvenanceResourceExecutorOperationalIdentityMax = 1024;

export const nodeProvenanceResourceExecutorRevisionMax = 1024;

export const nodeProvenanceResourceModelMax = 1024;

export const nodeProvenanceResourceProfileIdMax = 1024;

export const nodeProvenanceResourceProviderIdMax = 64;

export const nodeProvenanceResourceReceiptHashRegExp = new RegExp('^[0-9a-f]{64}$');
export const nodeProvenanceResourceRequestHashRegExp = new RegExp('^[0-9a-f]{64}$');
export const nodeProvenanceResourceRoleMax = 1024;



export const NodeProvenanceResource = zod.strictObject({
  "agent_configuration_revision_hash": zod.string().regex(nodeProvenanceResourceAgentConfigurationRevisionHashRegExp),
  "auth_mode": zod.string().min(1).max(nodeProvenanceResourceAuthModeMax),
  "executor_operational_identity": zod.string().min(1).max(nodeProvenanceResourceExecutorOperationalIdentityMax),
  "executor_revision": zod.string().min(1).max(nodeProvenanceResourceExecutorRevisionMax),
  "model": zod.string().min(1).max(nodeProvenanceResourceModelMax),
  "profile_id": zod.string().min(1).max(nodeProvenanceResourceProfileIdMax),
  "provider_id": zod.string().min(1).max(nodeProvenanceResourceProviderIdMax),
  "receipt_hash": zod.string().regex(nodeProvenanceResourceReceiptHashRegExp),
  "request_hash": zod.string().regex(nodeProvenanceResourceRequestHashRegExp),
  "role": zod.string().min(1).max(nodeProvenanceResourceRoleMax)
}).describe('Which agent answered for a node, as its own receipt recorded it.\n\nWho answered, not who wrote every byte of the value beside it. Usage is\nabsent because no receipt holds it: this proves what ran and what came out,\nnever what it cost, and how long it took is recorded beside the attempt.');

export type NodeProvenanceResource = zod.input<typeof NodeProvenanceResource>;
export type NodeProvenanceResourceOutput = zod.output<typeof NodeProvenanceResource>;

export const nodeRefusalOutputResourceValueBase64Max = 81920;

export const nodeRefusalOutputResourceValueHashRegExp = new RegExp('^[0-9a-f]{64}$');


export const NodeRefusalOutputResource = zod.strictObject({
  "value_base64": zod.string().max(nodeRefusalOutputResourceValueBase64Max),
  "value_hash": zod.string().regex(nodeRefusalOutputResourceValueHashRegExp)
}).describe('The redacted presentation of a schema-refused V3 agent node\'s own output.\n\nNot `NodeAnswerResource`, on purpose, and not just for the field name\n`answer` already carries: `answer` is served for every answer-bearing node\nkind (#238) -- Agent, Wait, Action, Subworkflow -- whose payloads have no\none shared byte bound, so `NodeAnswerResource.value_base64` stays\nunbounded. `refusal_output` is served for exactly one narrower case: a V3\nagent node\'s own execution, judged and refused (#664). Every executor\nadapter already refuses to hand the domain more than\n`MAXIMUM_AGENT_OUTPUT_BYTES_V2` bytes before that judgment runs, so this\nfield\'s own bound (`MAXIMUM_REFUSED_OUTPUT_BASE64_CHARACTERS`) rests on\nthat already-held cap rather than inventing a new one.\n\n`value_base64` is also not the exact judged bytes: `contracts.secret_redaction`\nruns over them before this resource is built, and any credential-shaped span\nis replaced (#664). Replacing a short credential with the longer\n`REDACTION_MARKER` can make the text longer than what was judged, never\nshorter than the cap alone would suggest -- so `MAXIMUM_REFUSED_OUTPUT_BASE64_CHARACTERS`\nis derived through the same redaction owner\'s own `maximum_redacted_length`,\nnot from the agent output cap directly, and a value at the cap with the\nshortest credential shape redacted out of it still fits. `value_hash`, by\ncontrast, is untouched -- it is the receipt\'s own hash of the original,\nunredacted bytes, so it still proves what the schema owner judged even\nthough the text shown beside it is a presentation of that judgment, not a\npreimage of the hash. A reader who rehashes `value_base64` to check it\nagainst `value_hash` is comparing two different things on purpose.');

export type NodeRefusalOutputResource = zod.input<typeof NodeRefusalOutputResource>;
export type NodeRefusalOutputResourceOutput = zod.output<typeof NodeRefusalOutputResource>;

export const nodeDetailResourceJobHashOneRegExp = new RegExp('^[0-9a-f]{64}$');

export const nodeDetailResourcePublicRunReferenceRegExp = new RegExp('^run1\\.[A-Za-z0-9_-]+$');



export const NodeDetailResource = zod.strictObject({
  "answer": zod.union([NodeAnswerResource,zod.null()]),
  "ended_at": zod.union([zod.string(),zod.null()]).optional(),
  "job_base64": zod.union([zod.string(),zod.null()]),
  "job_hash": zod.union([zod.string().regex(nodeDetailResourceJobHashOneRegExp),zod.null()]),
  "node_id": zod.string().min(1),
  "provenance": zod.union([NodeProvenanceResource,zod.null()]),
  "public_run_reference": zod.string().regex(nodeDetailResourcePublicRunReferenceRegExp),
  "refusal": zod.union([zod.string(),zod.null()]),
  "refusal_output": zod.union([NodeRefusalOutputResource,zod.null()]).optional(),
  "run_id": zod.string().min(1),
  "started_at": zod.union([zod.string(),zod.null()]).optional(),
  "state": zod.enum(['queued', 'working', 'needs_you', 'succeeded', 'failed', 'cancelled', 'interrupted']),
  "transcript": zod.union([AttemptTranscriptResource,zod.null()]).optional()
}).describe('One node of a run, answered the way an operator asks about it.\n\nFive answers, each allowed to be absent, because absence is itself the\nanswer. `job` is what the run really handed this node\'s provider, recomposed\nthrough the one owner that composed it; `job_hash` is the hash of exactly\nthose bytes and nothing more. It is \*\*not\*\* the receipt\'s `request_hash`,\nwhich frames the execution identity, the revision, the binding and the\noperational identity around the job -- a reader comparing the two would\nreject a job that is right. `provenance.request_hash` is the field that\nmeets the receipt.\n\n`answer` is what the node wrote, with the hash its own completion event\nkept. `provenance` is who did it. `refusal` is what stops the run here, and\nonly that: a node whose predecessor has simply not written yet carries no\njob and no refusal, because nothing has judged anything. Anything that would\nmean the store disagrees with itself -- a payload that no longer matches its\nhash, a pinned schema revision that is gone -- is not softened into a\nrefusal here; it leaves as durable corruption, loudly.\n\n`refusal_output` is deliberately not `answer`: `answer` is the value the run\naccepted, and a schema-refused value never was that (#664). It carries a\nredacted presentation of the bytes a schema owner judged and refused, read\nback from the content-addressed artifact the failure transaction kept, its\ncredential shapes replaced before this resource is built -- present only\nwhere such a judgment happened and something to keep survived it; every\nother refusal, including one this receipt family predates, answers with no\nsuch field rather than a guess. Its own type, `NodeRefusalOutputResource`,\nnames the redaction and the bound this field alone carries.\n\n`transcript` is the decoded events of the attempt that named one. The key\nis omitted when no attempt of this execution did -- never `\"transcript\":\nnull` -- so a node that stored nothing does not grow a new column. The\nstored document kind and raw bytes stay off this resource.');

export type NodeDetailResource = zod.input<typeof NodeDetailResource>;
export type NodeDetailResourceOutput = zod.output<typeof NodeDetailResource>;

export const NodeRailAttemptResource = zod.strictObject({
  "ordinal": zod.union([zod.literal(1),zod.literal(2)]),
  "state": zod.union([zod.enum(['PREPARED', 'POSSIBLY_RAN', 'CANCEL_REQUESTED', 'CANCELLED', 'INTERRUPTED', 'FAILED', 'SUCCEEDED']),zod.null()])
}).describe('The agent attempt one node tells a reader about.\n\nA succeeded attempt carries a state only where the run stands still on that\nnode\'s effect, owed the operator\'s reconciliation: everywhere else the same\ntransition that records the success moves the run past the attempt, and the\nnode\'s own `succeeded` is what says the work is done.');

export type NodeRailAttemptResource = zod.input<typeof NodeRailAttemptResource>;
export type NodeRailAttemptResourceOutput = zod.output<typeof NodeRailAttemptResource>;


export const nodeRailResourceReusedFromRunReferenceOneRegExp = new RegExp('^run1\\.[A-Za-z0-9_-]+$');
export const nodeRailResourceReusedFromRunReferenceDefault = null;
export const nodeRailResourceSourceDeclaredContextPackageHashOneRegExp = new RegExp('^[0-9a-f]{64}$');
export const nodeRailResourceSourceDeclaredContextPackageHashDefault = null;
export const nodeRailResourceSourceEventHashOneRegExp = new RegExp('^[0-9a-f]{64}$');
export const nodeRailResourceSourceEventHashDefault = null;
export const nodeRailResourceSourceReceiptHashOneRegExp = new RegExp('^[0-9a-f]{64}$');
export const nodeRailResourceSourceReceiptHashDefault = null;

export const NodeRailResource = zod.strictObject({
  "attempt": zod.union([NodeRailAttemptResource,zod.null()]),
  "node_id": zod.string().min(1),
  "reused_from_run_reference": zod.union([zod.string().regex(nodeRailResourceReusedFromRunReferenceOneRegExp),zod.null()]).default(nodeRailResourceReusedFromRunReferenceDefault),
  "source_declared_context_package_hash": zod.union([zod.string().regex(nodeRailResourceSourceDeclaredContextPackageHashOneRegExp),zod.null()]).default(nodeRailResourceSourceDeclaredContextPackageHashDefault),
  "source_event_hash": zod.union([zod.string().regex(nodeRailResourceSourceEventHashOneRegExp),zod.null()]).default(nodeRailResourceSourceEventHashDefault),
  "source_receipt_hash": zod.union([zod.string().regex(nodeRailResourceSourceReceiptHashOneRegExp),zod.null()]).default(nodeRailResourceSourceReceiptHashDefault),
  "state": zod.enum(['queued', 'working', 'needs_you', 'succeeded', 'failed', 'cancelled', 'interrupted'])
}).describe('Where one node of a run stands, said by the server rather than guessed.');

export type NodeRailResource = zod.input<typeof NodeRailResource>;
export type NodeRailResourceOutput = zod.output<typeof NodeRailResource>;

export const runCancellabilityResourceTargetNodeExecutionIdOneRegExp = new RegExp('^[0-9a-f]{64}$');


export const RunCancellabilityResource = zod.strictObject({
  "cancellable": zod.boolean(),
  "reason": zod.union([zod.enum(['between-nodes', 'waiting-for-you', 'node-runs-no-agent', 'already-cancelling', 'already-ended', 'answer-in-flight']),zod.null()]),
  "target_node_execution_id": zod.union([zod.string().regex(runCancellabilityResourceTargetNodeExecutionIdOneRegExp),zod.null()])
}).describe('Whether this V3 run can be operator-cancelled right now, said by the server.\n\n#439 D3 makes the server the owner of this predicate rather than letting the\ncockpit guess it from the rail. A cancellable run carries the\n`target_node_execution_id` the client fences its command on; a run that\ncannot be cancelled carries the closed reason token that names why, so the\ncockpit shows the operator sentence instead of a grey nothing.');

export type RunCancellabilityResource = zod.input<typeof RunCancellabilityResource>;
export type RunCancellabilityResourceOutput = zod.output<typeof RunCancellabilityResource>;

export const runForkOriginResourceForkHashRegExp = new RegExp('^[0-9a-f]{64}$');
export const runForkOriginResourcePublicRunReferenceRegExp = new RegExp('^run1\\.[A-Za-z0-9_-]+$');

export const runForkOriginResourceTerminalHashRegExp = new RegExp('^[0-9a-f]{64}$');


export const RunForkOriginResource = zod.strictObject({
  "fork_hash": zod.string().regex(runForkOriginResourceForkHashRegExp),
  "public_run_reference": zod.string().regex(runForkOriginResourcePublicRunReferenceRegExp),
  "restart_from_node_id": zod.string().min(1),
  "terminal_hash": zod.string().regex(runForkOriginResourceTerminalHashRegExp)
});

export type RunForkOriginResource = zod.input<typeof RunForkOriginResource>;
export type RunForkOriginResourceOutput = zod.output<typeof RunForkOriginResource>;

export const runForkSuccessorResourceForkHashRegExp = new RegExp('^[0-9a-f]{64}$');
export const runForkSuccessorResourcePublicRunReferenceRegExp = new RegExp('^run1\\.[A-Za-z0-9_-]+$');



export const RunForkSuccessorResource = zod.strictObject({
  "fork_hash": zod.string().regex(runForkSuccessorResourceForkHashRegExp),
  "public_run_reference": zod.string().regex(runForkSuccessorResourcePublicRunReferenceRegExp),
  "restart_from_node_id": zod.string().min(1)
});

export type RunForkSuccessorResource = zod.input<typeof RunForkSuccessorResource>;
export type RunForkSuccessorResourceOutput = zod.output<typeof RunForkSuccessorResource>;

export const runTerminalAnswerValueResourceValueBase64Max = 65536;

export const runTerminalAnswerValueResourceValueHashRegExp = new RegExp('^[0-9a-f]{64}$');


export const RunTerminalAnswerValueResource = zod.strictObject({
  "kind": zod.literal("value").meta({ title: 'Kind' }),
  "value_base64": zod.string().max(runTerminalAnswerValueResourceValueBase64Max),
  "value_hash": zod.string().regex(runTerminalAnswerValueResourceValueHashRegExp)
}).describe('The terminal node\'s own accepted answer, bounded for a listed run row.\n\nNot `NodeAnswerResource`: that one stays unbounded because a single node\ndetail read has no byte bound shared across every answer-bearing node\nkind (#238). This resource is served on every listed and read V3 run\ninstead (#1045).');

export type RunTerminalAnswerValueResource = zod.input<typeof RunTerminalAnswerValueResource>;
export type RunTerminalAnswerValueResourceOutput = zod.output<typeof RunTerminalAnswerValueResource>;

export const runTerminalAnswerOmittedResourceMaximumBytesMin = 0;
export const runTerminalAnswerOmittedResourceMaximumBytesMax = 9223372036854776000;



export const RunTerminalAnswerOmittedResource = zod.strictObject({
  "kind": zod.literal("omitted").meta({ title: 'Kind' }),
  "maximum_bytes": zod.int().min(runTerminalAnswerOmittedResourceMaximumBytesMin).max(runTerminalAnswerOmittedResourceMaximumBytesMax),
  "reason": zod.literal("too_large").meta({ title: 'Reason' })
}).describe('A terminal node did write an answer, but not the value on this row (#1045).\n\nA bare `null` here would read the same as a node that wrote nothing at\nall -- two different facts a reader could not tell apart. `reason` names\nwhich rule omitted it; `maximum_bytes` is the bound that rule enforced,\nso a reader is told the ceiling rather than left to guess it. The run\'s\nown node detail route still reads the value in full, never omitting it.');

export type RunTerminalAnswerOmittedResource = zod.input<typeof RunTerminalAnswerOmittedResource>;
export type RunTerminalAnswerOmittedResourceOutput = zod.output<typeof RunTerminalAnswerOmittedResource>;

export const runOrderResourceBytesMin = 0;
export const runOrderResourceBytesMax = 1048576;


export const runOrderResourceSchemaRevisionHashRegExp = new RegExp('^[0-9a-f]{64}$');


export const RunOrderResource = zod.strictObject({
  "bytes": zod.int().min(runOrderResourceBytesMin).max(runOrderResourceBytesMax),
  "name": zod.string().min(1),
  "schema_revision_hash": zod.string().regex(runOrderResourceSchemaRevisionHashRegExp)
}).describe('One order a V3 run was started with, told safely -- never its own bytes.\n\nAn order\'s material can be a secret a caller pasted by mistake, or an\nartifact up to `MAXIMUM_ARTIFACT_BYTES`, and this resource is served on\nevery listed run -- so it never echoes the order\'s bytes at all.\n\n`bytes` is how large the order\'s material is. `schema_revision_hash` is\nthe schema the order satisfies -- the workflow revision\'s own\n`WorkflowDeclaredOrderResourceV3` already names the `ref` a caller reads,\nso this is only the identity a caller compares it against.\n\nNo text preview travels here yet: a redacted glance at an order\'s material\nneeds the redaction owner #666 is building, and shipping a second one here\nahead of it would be a parallel copy of a decision that head has not made\nyet. #738\'s own body carries that as its named next step.');

export type RunOrderResource = zod.input<typeof RunOrderResource>;
export type RunOrderResourceOutput = zod.output<typeof RunOrderResource>;

export const runResourceV3AgentBindingSetHashRegExp = new RegExp('^[0-9a-f]{64}$');
export const runResourceV3AgentBindingsMax = 100;

export const runResourceV3CurrentNodeExecutionIdRegExp = new RegExp('^[0-9a-f]{64}$');

export const runResourceV3ForkSuccessorsDefault = [];
export const runResourceV3ForkSuccessorsMax = 100;

export const runResourceV3LatestEventCursorOneRegExp = new RegExp('^event1\\.[A-Za-z0-9_-]+\\.[1-9][0-9]*$');

export const runResourceV3OrdersMax = 100;

export const runResourceV3PublicRunReferenceRegExp = new RegExp('^run1\\.[A-Za-z0-9_-]+$');
export const runResourceV3RunConfigurationRevisionHashRegExp = new RegExp('^[0-9a-f]{64}$');

export const runResourceV3StateVersionMin = 0;
export const runResourceV3StateVersionMax = 9223372036854776000;

export const runResourceV3TerminalHashOneRegExp = new RegExp('^[0-9a-f]{64}$');
export const runResourceV3WorkItemReferenceOneMax = 1024;


export const runResourceV3WorkflowRevisionHashRegExp = new RegExp('^[0-9a-f]{64}$');


export const RunResourceV3 = zod.strictObject({
  "agent_binding_set_hash": zod.string().regex(runResourceV3AgentBindingSetHashRegExp),
  "agent_bindings": zod.array(AgentBindingResourceV2).max(runResourceV3AgentBindingsMax),
  "answer": zod.union([zod.union([RunTerminalAnswerValueResource,RunTerminalAnswerOmittedResource]),zod.null()]).optional(),
  "cancellation": RunCancellabilityResource,
  "current_node_execution_id": zod.string().regex(runResourceV3CurrentNodeExecutionIdRegExp),
  "current_node_id": zod.string().min(1),
  "ended_at": zod.union([zod.string(),zod.null()]).optional(),
  "fork_origin": zod.union([RunForkOriginResource,zod.null()]).optional(),
  "fork_successors": zod.array(RunForkSuccessorResource).max(runResourceV3ForkSuccessorsMax).default(runResourceV3ForkSuccessorsDefault),
  "latest_event_cursor": zod.union([zod.string().regex(runResourceV3LatestEventCursorOneRegExp),zod.null()]),
  "node_rail": zod.array(NodeRailResource).min(1),
  "orders": zod.array(RunOrderResource).max(runResourceV3OrdersMax),
  "public_run_reference": zod.string().regex(runResourceV3PublicRunReferenceRegExp),
  "refusal_output": zod.union([NodeRefusalOutputResource,zod.null()]).optional(),
  "run_configuration_revision_hash": zod.string().regex(runResourceV3RunConfigurationRevisionHashRegExp),
  "run_id": zod.string().min(1),
  "started_at": zod.union([zod.string(),zod.null()]).optional(),
  "state": zod.enum(['STARTED', 'WAITING_RECONCILIATION', 'WAITING_INPUT', 'COMPLETED', 'FAILED', 'CANCELLED']),
  "state_version": zod.int().min(runResourceV3StateVersionMin).max(runResourceV3StateVersionMax),
  "terminal_hash": zod.union([zod.string().regex(runResourceV3TerminalHashOneRegExp),zod.null()]),
  "work_item_reference": zod.union([zod.string().max(runResourceV3WorkItemReferenceOneMax),zod.null()]).optional(),
  "workflow_format_version": zod.literal(3).meta({ title: 'Workflow Format Version' }),
  "workflow_name": zod.string().min(1),
  "workflow_revision_hash": zod.string().regex(runResourceV3WorkflowRevisionHashRegExp)
}).describe('One run as it reads back, ended on the sink its author declared.\n\nA run ends on an \*\*agent\*\* sink, because #194 H1b lifted the terminal\ncondition off the subworkflow node onto the run; `validate_state_shape`\ntherefore ties the terminal hash to the run\'s own state and to no node kind.\n\nTwo fields are absent on purpose rather than empty. There is no\n`agent_attempts` array listing every attempt on the current node: repeating\nit next to the rail would be a second owner of the same fact. The rail\'s\n`attempt` is the one the reader is told -- including on a failed terminal\nnode, where the snapshot keeps the failed attempt so a list read names the\nsame ending the event stream names. Surfacing an `agent_attempts` array is\nstill its own head; it is a named gap here, not a claim.\n\nA run does reach WAITING_INPUT, and `current_node_id` with the rail is what\nsays so: the rail marks that node as the one owing a person a move. What is\ndeliberately absent is a `waiting` block restating the question, because a\nWait node\'s prompt and answer schema are the document\'s and are read from\nthe workflow revision this run names, not copied onto the run. Projecting the\nquestion onto the run resource is a named gap, not a claim.');

export type RunResourceV3 = zod.input<typeof RunResourceV3>;
export type RunResourceV3Output = zod.output<typeof RunResourceV3>;

export const RunListRowResource = zod.strictObject({
  "kind": zod.literal("run").meta({ title: 'Kind' }),
  "run": RunResourceV3
}).describe('A listed row for one run whose own projection could be told.');

export type RunListRowResource = zod.input<typeof RunListRowResource>;
export type RunListRowResourceOutput = zod.output<typeof RunListRowResource>;

export const versionedRunPageResourceNextAfterOneRegExp = new RegExp('^run1\\.[A-Za-z0-9_-]+$');


export const VersionedRunPageResource = zod.strictObject({
  "items": zod.array(zod.union([RunListRowResource,DefectiveRunRowResource])),
  "next_after": zod.union([zod.string().regex(versionedRunPageResourceNextAfterOneRegExp),zod.null()])
});

export type VersionedRunPageResource = zod.input<typeof VersionedRunPageResource>;
export type VersionedRunPageResourceOutput = zod.output<typeof VersionedRunPageResource>;

